# 가격·하드웨어 최초 실측 결과

2026-09-14 14:26 KST 시작, `.env`의 `gpt-4o-mini`를 기존 `OpenAISpecJudge`로 호출했다.
가격·RAM 규칙은 준비한 사례를 모두 통과했지만, LLM은 정보가 부족할 때 판정을 단정하는 문제가 확인됐다.
실행 과정에서 제품 코드와 프롬프트, 기대 라벨을 수정하지 않았다.

## 실행 범위와 결과

평가 질문 50개 중 LLM 30개는 각각 2회, 규칙 처리 20개는 1회 실행했다.
한 질문(H30)에 후보 3개가 있어 LLM 판정은 64건이다. 자연어 질문은 설명용이며
현재 판정기의 실제 계약인 정규화 사양을 입력했다. 질문 이해 능력이나 전체 추천 품질 점수는 아니다.

| 항목 | 결과 |
|---|---|
| LLM 후보별 기대 상태 일치 | 42/64 (65.6%) |
| LLM 질문 전체 통과 | 38/60회 (63.3%), 고유 질문 19/30개 |
| 잠정 성능·제안 정책 4개 제외, LLM 후보별 일치 | 36/56 (64.3%) |
| 가격·RAM·누락 처리 등 규칙 사례 | 20/20 (100%) |
| LLM의 미달 → 충족 오판 | 0/10 |
| LLM의 판단 불가 → 충족 오판 | 4/26 (15.4%), H13·H15 각각 2회 |
| LLM의 판단 불가 → 미달 단정 | 16/26 (61.5%) |
| LLM의 판단 불가 유지 | 6/26 (23.1%) |
| 실제 LLM API 오류 | 0/60회 |
| 2회 실행 사이 상태가 달라진 질문 | 0/30개 |
| 실제 Steam 조회·파싱 | 3/3 성공 |

같은 질문을 반복한 표본이므로 64건은 서로 독립된 64질문이 아니다.
일치율은 이 합성 평가셋에 한정되며 실제 사용자 분포의 정확도로 일반화할 수 없다.
라벨은 실행 전에 에이전트가 작성했으며 전문가가 검수한 성능 정답셋은 아니다.
별도 판정기나 동일 모델로 정답을 생성하지 않았다.

## 주요 실패 및 근거 품질

| 사례 | 기대 / 실제 | 관찰 |
|---|---|---|
| H07 CPU만 제공 | met / unmet | 동일 Ryzen 5 5600은 충족인데 미제공 GPU를 근거로 삼았다. 설명은 “RTX 3060은 … 이상입니다”로 상태와도 모순된다. |
| H08 `i5` | unknown / unmet | 세대가 없는데 구체적인 i5-12400을 사용자 CPU처럼 설명한다. |
| H09 `GeForce`, H11 가상 GPU | unknown / unmet | 제공하지 않은 RTX 3060을 사용자 GPU처럼 설명하고, 충족 설명과 미달 상태가 어긋난다. |
| H10 모델 불명 내장그래픽 | unknown / unmet | 내장 GPU 모델을 확인하지 않고 미달로 단정한다. 현재 프롬프트의 내장 GPU 일반화 규칙과 불명 모델 규칙도 정리할 필요가 있다. |
| H12 가상 CPU | unknown / unmet | 존재하지 않는 부품의 성능을 미달로 단정한다. |
| H13 최소 조건이 “고성능 그래픽카드” | unknown / met | 비교 기준 모델·기능이 없는데 충족으로 통과시킨다. |
| H15 GPU 충족 + CPU `i5` | unknown / met | GPU만 보고 통과시키며 모호한 CPU를 반영하지 않는다. |
| H16 CPU 충족 + GPU `GeForce` | unknown / unmet | 모델 불명 GPU를 미달로 단정한다. |
| H17 사용자 CPU, 게임은 GPU 조건만 있음 | unknown / unmet | 공통 비교 항목이 없는데 미달 판정과 GPU 충족 설명을 내놓는다. |
| H27 CPU만 미달 (잠정 성능 사례) | unmet / unmet | 상태는 맞지만 “RTX 3060은 최소 사양 'Intel Core i5-12400' 이상입니다”라는 CPU/GPU 혼합 설명을 한다. 상태 일치율만으로 근거 품질을 보증할 수 없다. |
| H28 Laptop GPU, TGP 불명 | unknown / unmet | 제안한 보수적 정책과 불일치한다. 이 라벨은 팀 검토가 필요하므로 확정 오류로 단정하지 않고 주 점수에서 분리했다. |

위 표는 1회차 원문을 수동 검토한 사례다. 상태 불일치는 2회차에도 동일했다.
문장 정확성을 전수 자동 채점한 점수는 아니다. API가 반환한 판정 객체는 `raw_verdicts`에 보존했다.
원본 HTTP/Responses 전체 응답이나 토큰 사용량은 보존하지 않았다.

## 실제 Steam 조회

별도의 실제 호출에서 다음 App ID의 가격·사양을 정규화했다.
이 값은 조회 시점의 결과이며 가격 평가의 고정 정답으로 사용하지 않았다.

| App ID | 조회 결과 | 최소 RAM |
|---|---|---|
| 570 | 무료 플래그 true, 가격 필드 없음 | 4GB |
| 620 | KRW 11,000원 | 2GB |
| 1245620 | KRW 64,800원, 최소·권장 사양 모두 조회됨 | 12GB |

실제 Steam 조회는 연결·파싱 확인이다. 웹페이지와의 독립 대조나 이 실제 사양으로 LLM까지 이어지는
전체 실환경 평가를 수행한 것은 아니다. 합성 고정 사양의 LLM 결과와 분리해야 한다.

## 다음 개선 우선순위

1. 사용자 항목과 요구 항목의 교집합을 먼저 확인하고, 비교 항목 없음·모호한 모델을 `unknown`으로 처리한다.
2. CPU/GPU별 상태와 사용한 사용자/최소 사양을 구조화해 반환하게 하고, 전체 상태를 코드로 합성한다.
3. `unmet`인데 “이상입니다”라고 설명하거나 CPU와 GPU를 비교하는 근거 오류를 차단한다.
4. 변경 뒤 같은 고정셋으로 비교하고, 새로운 비공개 사례와 독립 벤치마크 검토 라벨을 추가한다.

현재 작업은 검증이므로 제품 로직과 프롬프트를 개선하지 않았다.

## 재현 및 파일

- [질문 50개](questions.md), [정규화 입력과 기대값](dataset.json)
- [실행 메타데이터](runs/baseline/metadata.json), [건별 원문 판정](runs/baseline/results.jsonl)
- [집계](runs/baseline/summary.json), [실제 Steam 결과](runs/baseline/steam_live.json)
- [평가 실행기](run_eval.py), [실행 방법 및 범위](README.md)

평가 후 실행기에 빈/중복 응답을 정답으로 오인하지 않도록 검사를 추가했다.
저장된 baseline에는 누락·중복 ID가 없어 점수 변화가 없음을 확인했으며 추가 유료 재실행은 하지 않았다.
기존 테스트 46개와 평가 실행기 오프라인 검사 4개, 총 50개를 통과했다.
이는 위 LLM 품질 사례 50개가 전부 통과했다는 뜻이 아니다.

---

# v2 개선 결과 (2026-09-14, 판정기 구조 변경 후)

baseline의 개선 우선순위 1~3을 반영하고 같은 50문항을 2회씩 재실행했다 (`runs/v5`, 중간 시도는 `runs/v2`~`v4`).
모델은 그대로 `gpt-4o-mini`다.

## 바꾼 것

| 구분 | 변경 | 해결한 실패 |
|---|---|---|
| 프롬프트 | reason 예시 문장 삭제 | 예시의 "RTX 3060"을 사용자 부품처럼 베끼던 H07~H09 |
| 프롬프트 | 미확인 부품·VRAM 미명시·세대 차이 규칙 추가 | 실제 게임에서 RTX 3060을 VRAM 미달로 오판, i5-1240P를 i5-8400 미달로 오판 |
| 출력 구조 | LLM은 부품별 `{component, status, note}`만 내고, 전체 판정·이유 문장은 코드가 합성 | GPU만 보고 통과(H15), 미달인데 "이상입니다"(H27), CPU/GPU 혼합 설명 |
| 코드 | 사용자·게임 양쪽에 값이 있는 항목만 `compare`로 지정. 공통 항목 없으면 LLM 없이 `unknown` | H17, H18 |
| 코드 | 사용자 부품에 3자리 이상 모델 번호(또는 Iris/UHD 등 내장 계열명)와 알려진 제품 계열이 없으면 LLM 없이 `skipped` "모델 확인 필요" | H08~H12, H15, H16 |
| 코드 | Apple Silicon은 `skipped` "Mac 사양 판정 미지원" (Steam `pc_requirements`는 Windows 기준) | — |

## 라벨 변경 (팀 방침)

모델명 없는 사용자 부품(H08 `i5`, H09 `GeForce`, H10 `내장그래픽`, H15, H16)과 알려진 계열이 아닌 부품(H11, H12)은
`unknown`(제외) 대신 **`skipped`(통과 + 최소·권장 사양 표시 + 되묻기)**로 처리하기로 했다. 이유:
"내장그래픽"만 말한 사용자에게 추천 0개를 주는 것보다 사양을 보여주고 판단을 맡기는 편이 낫고,
GPU를 무시하고 CPU만 비교하면 "미달인데 충족"이 생기므로 무시는 안 된다. 이 7개는 라벨을 `skipped`로 바꾸고 `expects_llm=false`로 두었다.

## 결과

| 항목 | baseline | v5 |
|---|---|---|
| 전체 후보별 일치 | 62/84 (73.8%) | **75/75 (100%)** |
| LLM 후보별 일치 | 42/64 (65.6%) | 46/46 |
| 규칙 처리 | 20/20 | 29/29 |
| 미달 → 충족 오판 | 0 | 0 |
| 판단 불가 → 충족 오판 | 4 | 0 |
| 판단 불가 → 미달 단정 | 16 | 0 |
| 2회 실행 불일치 | 0 | 0 |
| LLM 호출 사례 | 30 | 23 (7개는 코드가 먼저 처리) |

**원래 라벨 기준으로 읽으면** 위 7개는 `unknown` 기대에 `skipped`가 나온 것이라 68/75 (90.7%)다. 두 수치의 차이는 정확도가 아니라 방침 차이다.

## 남은 한계

- 합성 요구 사양 기준이며 실제 Steam HTML 다양성은 별도 smoke(실제 5개 게임 × 3케이스)로만 확인했다.
- `unknown`을 유지하는 능력(H13, H14, H28, H30-3)은 8/8이지만 표본이 적다.
- 알려진 제품 계열 목록은 수동 관리다. 목록에 없는 실제 부품(예: Quadro 외 워크스테이션 제품)은 판정 대신 사양 표시로 빠진다.
- 이유 문장은 코드가 만들어 사실 오류는 없지만, `note`(LLM이 쓴 30자 근거)의 품질은 채점하지 않았다.
